# nix-direnv watches flake.nix and flake.lock, but not the modules they import,
# so changes under nix/flake-modules would not reload the shell on their own.
watch_file nix/flake-modules/*.nix

use flake . --override-input devenv-root "file+file://"<(printf %s "$PWD")
